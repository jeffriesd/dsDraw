define ls() {
a = [8, 1, 3, 2, 5, 6];
e = 2;
ls = [3, 2, 5, 6];

z = array(a);
}


-- buckset bug 
define bs() {
  n = 1;
  ids = [0] * n;
  xs = [0] * n;
  for (i = 0; i < n; i = i + 1) {
    ids[i] = "c" + str(i);
    xs[i] = $(ids[i], conn());
  }
}
