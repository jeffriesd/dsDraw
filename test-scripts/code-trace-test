

-- test 1

define f() {
  x = 0; 
  y = x + x;
} 

f(); 

-----------


-- test 2

define fib(x) {
  if (x < 2) { return x; }
  return fib(x-1) + fib(x- 2);
}

xs = [0, 0, 0];
for (i = 0; i < 3; i = i + 1) {
  xs[i] = fib(i);
}

-----------
